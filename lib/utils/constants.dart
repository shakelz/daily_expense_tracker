/// Global constants for the expense tracker app
class Constants {
  /// Flag to indicate if the transaction form should open on app launch
  /// Used when bubble is tapped while app is killed
  static bool showFormOnLaunch = false;
}
